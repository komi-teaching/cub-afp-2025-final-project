# local-lang

A small imperative language where references can be marked
as `local` to prevent escape.

Note: we noticed at the last minute that there's a bug in the
semantics, and so most standard theorems aren't provable.
Much of the code is still committed in branches, though.
